# tetrng

Find the first RNG seed that produces a Tetris piece sequence.

Build and run the parallel C implementation:

```sh
make
./tetrng JILTOSZJ
```

Run the Python reference:

```sh
./tetrng.py JILTOSZJ
```
